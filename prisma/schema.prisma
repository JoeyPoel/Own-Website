model Profile {
  id           String @id @default(uuid())
  name         String
  role         String
  location     String
  availability String
}

model Service {
  id          String @id @default(uuid())
  title       String
  description String
  timeframe   String
}

model Project {
  id         String   @id @default(uuid())
  order      Int?     @default(0)
  title      String
  category   String
  tagline    String
  stack      String[]
  highlights String[]
  linkLabel  String?
  linkUrl    String?
  image      String
  status     String?
}

model Testimonial {
  id      String @id @default(uuid())
  quote   String
  author  String
  role    String
  company String
}

model Inquiry {
  id          String   @id @default(uuid())
  name        String
  email       String
  projectType String
  budget      String
  timeline    String
  message     String
  company     String?
  phone       String?
  links       Json?
  status      String   @default("pending")
  createdAt   DateTime @default(now())
}
